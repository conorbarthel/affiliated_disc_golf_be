<%= form_with url: disc_path(@disc), method: :patch, local: true do |form| %>
  <p>Update Disc:</p>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.label :plastic %>
  <%= form.text_field :plastic %>
  <%= form.label :speed %>
  <%= form.number_field :speed %>
  <%= form.label :quantity %>
  <%= form.number_field :quantity %>

  <%= form.submit "Submit" %>
<% end %>
