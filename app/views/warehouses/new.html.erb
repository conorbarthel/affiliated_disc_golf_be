<%= form_with url: warehouses_path(@warehouse), method: :post, local: true do |form| %>
  <p>New Warehouse:</p>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.submit "Submit" %>
<% end %>
